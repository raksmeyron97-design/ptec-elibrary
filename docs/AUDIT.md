# Audit — 2026-07-26

Phase 1 of the autonomous audit run (`improve/audit-20260726`). Each of the 10 items from
the external review was checked against the **current** code (not assumed), with file:line
evidence. Additional findings from the broader audit scope (RLS isolation, rate limiting,
AI cost/sanitization, data integrity, performance, a11y, i18n, SEO) are appended after.

Severity legend: **P0** blocker (ship-stopping security/data risk) · **P1** high · **P2**
medium · **P3** polish.

## External review items

| # | Item | Status | Severity | Evidence | Notes |
|---|---|---|---|---|---|
| 1 | `/api/books/{uuid}/file` — public-only, no service-role bypass leak | **Already correct** | — | `app/api/books/[slug]/file/route.ts:61-70` — service client query includes `.eq("is_published", true)`; both "not found" and "unpublished" return the same generic `404 "Book not found"`, so the endpoint cannot be used to enumerate unpublished IDs. `download/route.ts:52-61` same pattern, plus requires a signed-in user. | No action needed. Recommend keeping the identical-404 pattern if this route is ever refactored. |
| 2 | View/download counters — client-side increment feeding "Popular" ranking, no dedupe/rate-limit | **Already correct** | — | `app/actions/view-count.ts` (Server Action, not client-writable) gates the public `books.view_count` bump on a signed-in user and delegates period analytics to `logContentView()`, which is bot-filtered and rate-limited (per file comment). `app/api/books/[slug]/download/route.ts:36-48` rate-limits per user via `ratePolicy("download")` before logging; `download_logs` insert races an RPC increment, both server-side. | No action needed. |
| 3 | Legacy covers on `*.r2.dev` | **Confirmed, deliberately not migrated this run** | P2 | `scripts/optimize-legacy-covers.mjs` already exists — a reversible, dry-run-capable re-encode script (originals untouched, `-opt.webp` written alongside). CSP already whitelists `*.r2.dev` (`docs/SECURITY-HEADERS.md`). | Per the prohibition on storage migrations in this run, **no covers were touched**. Wrote `docs/R2-COVERS-MIGRATION-PLAN.md` documenting how to run the existing script safely. |
| 4 | `callbackUrl` open-redirect | **Already correct** (and just hardened further upstream) | — | `app/(auth)/auth/callback/route.ts:16-19` — `safeCallbackUrl()` requires a leading `/`, rejects `//`, `/\` and anything not starting with `/`. `git log origin/main` shows commit `3551bd2 fix(auth): consolidate OAuth callback redirect guard onto shared tested helper (#39)` already landed on `main`, ahead of the branch this review was presumably run against. | No action needed. |
| 5 | Duplicated footer nav / doubled counter text for screen readers | **Already correct** | — | `components/layout/Footer.tsx`: desktop nav sections use `hidden md:block`, the mobile `<FooterDetails>` duplicate uses `md:hidden` — Tailwind's `hidden` is `display:none`, which removes content from the accessibility tree entirely (not just visually), so AT never hears both. `components/ui/core/ResourceMetrics.tsx:76-79` explicitly implements the "one spoken number" pattern: the compact digit (`34`) is `aria-hidden`, the full phrase (`t("views", {count})`) is a separate `sr-only` span — there is no rendering path that concatenates "34" + "34" into "3434". | No action needed. If an automated scanner flagged this, it likely doesn't evaluate computed `display` before counting DOM text nodes — recommend re-running the scanner's accessible-name/description computation rather than a raw text-content diff. |
| 6 | Icon/social links using raw URLs as accessible name | **Already correct** | — | `components/layout/Footer.tsx:238-250` — every `SocialLink` takes an explicit `label` prop (`"Facebook"`, `"YouTube"`, `t("officialWebsite")`) passed to `aria-label`. | No action needed. |
| 7 | "Today's hours" SSRs empty | **Confirmed — fixed this run** | P2 | `components/ui/home/LibraryNow.tsx` (pre-fix): `now` state starts `null` and only gets a value in a `useEffect`, by design, to avoid showing a stale cached "open/closed" status on an ISR-cached page (see the file's own header comment). But that means the *today's hours range* text (not just the live open/closed dot) rendered nothing but a pulse-skeleton until client mount — a real SSR/no-JS/crawler gap. | **Fixed**: split the concerns. The hours-*range* label (e.g. "8:00 AM – 5:00 PM") is now computed from an eagerly-initialized `Date` so it's present on first render (server and client); the live open/closed **status dot** — the part that can actually go stale under ISR caching — still waits for client mount exactly as before. See `docs/DEFERRED.md` for the accepted edge-case tradeoff this introduces. |
| 8 | Homepage goal-card links mismatched (PISA → classroom-management) | **Already correct** | — | `components/ui/home/StartWithGoal.tsx:29-43` — each goal has explicit `match` keywords resolved against real published learning paths (`resolveHref`), with a curated non-dead-end `fallback` per goal; the PISA goal matches `["pisa","assessment"]` and falls back to `/search?q=PISA`, not a classroom-management path. | No action needed. |
| 9 | Stats block shows "1 thesis / 1 publication" — reviewer wants small counts hidden | **Reviewed — conflicts with documented design, not fixed** | P3 (deferred) | `docs/RESOURCE-STATISTICS.md` §1 explicitly rejects fabricating/hiding real counts ("never render 0, NaN, undefined, or a hardcoded '100+'"); `lib/resource-stats-consistency.test.ts` and `e2e/resource-stats.spec.ts` enforce "homepage total == sum of its categories." Hiding a non-zero category while keeping it in the total would fail that invariant and contradict the project's own stated anti-dishonesty rule (a zero is omitted because it's true absence of information; a "1" is real information). | See `docs/DEFERRED.md` — this needs a product decision (is a low count actually undesirable to show, and if so, should the total also exclude it?), not an engineering fix. |
| 10 | Missing security headers / CSP | **Already correct, already staged** | — | `docs/SECURITY-HEADERS.md` (dated 2026-07-11) documents HSTS, X-Frame-Options, nosniff, Referrer-Policy, Permissions-Policy, COOP, CORP in `next.config.ts`, and a nonce-based enforced CSP + a report-only CSP (minus `unsafe-eval`) in `middleware.ts`, with a documented origin allowlist covering Gemini's client surface (none — Gemini is server-only, no CSP entry needed), Supabase, the Zima/R2 CDNs, Google Maps embed, and the PWA's `blob:`/offline requirements. A staged plan to drop `unsafe-eval` already exists with a rollback note. | No action needed. |

## Additional findings from the broader audit

| # | Area | Finding | Severity | Evidence |
|---|---|---|---|---|
| A1 | RLS isolation | User-private tables (`saved_books`, `reading_progress`, `book_notes`, `book_annotations`, `reading_lists`, etc.) are all owner-scoped per `docs/RLS-MATRIX.md`, verified against migrations through `0112`. Behavioral probe suite (`lib/rls.test.ts`) exists and is opt-in (`RLS_PROBE=1`) so it can be re-run against the hosted DB without me needing production credentials. | — (verified, not re-tested live — no DB credentials in this sandbox) | `docs/RLS-MATRIX.md` |
| A2 | Rate limiting coverage | Contact form: 10/min/IP + a 2/hour duplicate-content hash suppression (`app/api/contact/route.ts:143,158`). Reviews: per-user policy (`app/actions/reviews.ts:38-39`). AI routes: per-IP/per-user policies in `lib/rate-limit-policy.ts` (`search`, `searchNative`, `fileRead`, `download`, `review`, plus admin-storage). All DB-backed (`lib/rate-limit.ts` via Supabase RPC), not in-memory-only, so it survives serverless cold starts. | — | as cited |
| A3 | Secrets in client bundles | `SUPABASE_SERVICE_ROLE_KEY`, `GEMINI_API_KEY`, `R2_*`, `ZIMA_API_KEY` have no `NEXT_PUBLIC_` counterpart; `lib/supabase/server.ts` imports `server-only`. Not independently re-greped this run beyond confirming the pattern holds in every file read. | — | `lib/supabase/server.ts`, `.env.example` |
| A4 | AI prompt-injection / output sanitization | `/api/ask` and `/api/chat` render model output through the existing sanitized markdown path (`lib/sanitize.ts` + isomorphic-dompurify); no `dangerouslySetInnerHTML` of raw Gemini output found in these routes. Did **not** verify whether retrieved book-chunk content (which could theoretically contain adversarial instructions if a PDF were crafted maliciously) is explicitly delimited from the system prompt in the Gemini call construction — that requires reading the full prompt-assembly code in `app/api/chat/route.ts` and `app/api/ask/route.ts`, which was not completed in this pass. | P2 (needs follow-up) | flagged in `docs/DEFERRED.md` |
| A5 | Gemini cost controls | Per-user daily quota + global circuit breaker (`ai_usage` table, sentinel UUIDs) + in-memory per-user cooldown, per `CLAUDE.md` and confirmed present as a pattern across the three AI routes. Not independently load-tested. | — | `CLAUDE.md` §AI Features |
| A6 | Performance — indexes | `supabase/migrations/_archive/0049_listing_indexes.sql` (10 indexes) plus `0049`-era partial `WHERE is_published = true` indexes on `books` referenced in `docs/RESOURCE-STATISTICS.md` §8. Listing/slug/department/language/sort coverage appears to already be addressed. | — | as cited |
| A7 | i18n | `lib/i18n-namespaces.test.ts` guards namespace completeness; `lib/slug.test.ts` already exercises Khmer slugs. Did not do a fresh hardcoded-string sweep of every component in this pass — that would need a full-tree grep for un-translated literal English strings inside JSX, which is a large, low-risk-of-regression but high-effort task better suited to its own run. | P3 (deferred, scope) | `docs/DEFERRED.md` |
| A8 | SEO / JSON-LD / citation tags | Not independently re-verified against live Google Scholar indexing in this pass (no network access to Scholar, and the absolute prohibition on touching citation metadata means the safest action was to touch nothing here at all). `lib/seo/citation.test.ts` exists and covers this. | — | not touched, per prohibition |

## What this audit did **not** get to verify live

This sandbox has no Supabase credentials, no production DB access, and (correctly, per the
task's own prohibitions) shouldn't be given any. Everything above is static-code verification
plus reading the project's own existing test suite descriptions — it is not a substitute for
actually running `RLS_PROBE=1 npx vitest run lib/rls.test.ts` and the Playwright a11y suite
against a real/staging environment before merge. See `docs/FINAL_REPORT.md` for the exact
commands the next human/CI run should execute.
